---
import projects from "../../collections/projects.json";
import Button from "../button.astro";
import Project from "../project.astro";
---

<section class="max-w-4xl mx-auto px-7 lg:px-0 py-14 lg:py-20 text-center">
  <h2
    class="text-2xl font-bold leading-10 tracking-tight text-neutral-900 
           dark:text-neutral-100 sm:text-3xl sm:leading-10 md:text-4xl md:leading-14">
    Mis Proyectos
  </h2>
  <p class="mb-6 text-base text-neutral-600 dark:text-neutral-400 sm:text-lg md:text-xl lg:px-20  ">
    Aquí tienes algunos de mis proyectos recientes. Siempre estoy trabajando en algo nuevo, 
    ¡así que vuelve a revisar con frecuencia!
  </p>
  <div
    class="grid items-stretch w-full grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-7 mt-7 lg:mt-10 "
  >
    {
      projects.map((project) => {
        return (
          <Project
            name={project.name}
            description={project.description}
            image={project.image}
            url={project.url}
          />
        )
      })
    }
  </div>

  <div class="flex items-center justify-center w-full py-5 lg:py-10  ">
    <Button text="Ver todos mis proyectos" link="/projects" />
  </div>
</section>
